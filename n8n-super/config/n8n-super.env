# n8n-super 启动自定义配置（会被 /docker-entrypoint-extra.sh 在启动时加载）

# 公开 pip 镜像源（清华 TUNA）
N8N_PIP_INDEX_URL="https://pypi.tuna.tsinghua.edu.cn/simple"
N8N_PIP_TRUSTED_HOST="pypi.tuna.tsinghua.edu.cn"

# PythonFunction 运行时自动安装依赖
N8N_PYTHON_AUTO_INSTALL="true"

# 使用 requirements 文件方式（该文件会通过 docker-compose 挂载到容器内）
N8N_PYTHON_REQUIREMENTS_FILE="/home/node/.n8n/requirements.txt"

# 多人/多工作流推荐：按依赖 hash 独立 venv 缓存目录
N8N_PYTHON_VENV_CACHE_DIR="/home/node/.n8n/pyenvs"

# 可选：启动时清理旧 venv 缓存（默认不清理）
# N8N_PYTHON_VENV_CACHE_CLEANUP="true"
# N8N_PYTHON_VENV_CACHE_TTL_DAYS="30"

# 如需额外 pip 参数（代理/额外源/证书等）可在此设置
# N8N_PYTHON_PIP_EXTRA_ARGS=""
