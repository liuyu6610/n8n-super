# config/build.env
#
# Build-time configuration for building n8n-super image.
# - This file is intended to be read by build scripts/CI to pass docker build-args.
# - Values can be empty; empty means "do not set" and docker/pip will use defaults.
#
# Image build args
ARGOCD_VERSION=v2.13.3

# Optional: override community nodes install list at build time (space-separated npm packages)
# If empty, Dockerfile will install from config/community-nodes.list
COMMUNITY_NODES=

# Pip settings for build-time pip installs (base venv preinstall)
PIP_INDEX_URL=
PIP_EXTRA_INDEX_URL=
PIP_TRUSTED_HOST=
PIP_DEFAULT_TIMEOUT=
