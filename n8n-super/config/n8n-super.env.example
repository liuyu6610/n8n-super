# n8n-super 启动自定义配置示例（被 docker-entrypoint-extra.sh 加载）
# 用法：复制为 config/n8n-super.env，然后在 compose 里挂载到容器内并设置 N8N_SUPER_CONFIG_FILE

# --- 企业 pip 源（推荐） ---
# N8N_PIP_INDEX_URL="https://pip.xxx.com/simple"
# N8N_PIP_EXTRA_INDEX_URL=""
# N8N_PIP_TRUSTED_HOST="pip.xxx.com"
# N8N_PIP_DEFAULT_TIMEOUT="30"

# --- PythonFunction 运行时自动装依赖（推荐） ---
# 开启后 python3 wrapper 会在每次执行前按需 pip install
N8N_PYTHON_AUTO_INSTALL="true"

# 方式1：包列表（空格分隔）
# N8N_PYTHON_PACKAGES="requests==2.32.3 python-gitlab"

# 方式2：requirements 文件（需要你额外挂载到容器内）
# N8N_PYTHON_REQUIREMENTS_FILE="/home/node/.n8n/requirements.txt"

# --- 多人/多工作流推荐：按依赖 hash 独立 venv（互不影响 + 缓存） ---
# N8N_PYTHON_VENV_CACHE_DIR="/home/node/.n8n/pyenvs"

# 可选：启动时清理旧 venv 缓存（按目录 mtime）
# N8N_PYTHON_VENV_CACHE_CLEANUP="true"
# N8N_PYTHON_VENV_CACHE_TTL_DAYS="30"

# pip 额外参数（如公司私有源/代理/证书等）
# N8N_PYTHON_PIP_EXTRA_ARGS="--trusted-host pip.xxx.com --index-url https://pip.xxx.com/simple"
